# MyShell
A Mini Unix Shell for CS-12600 System Programming in C. by Ricardo Losano

to compile the source code, you must run the command "make" on the root file. Afterwards, go to the bin/ folder and run "./myshell".
Necessary Dependencies (for arch linux): coreutils, cmake

Example commands:
ls
cd ../
exit
quit
